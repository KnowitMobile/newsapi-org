import XCTest
@testable import NewsAPI_org

final class NewsAPI_orgTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        //XCTAssertEqual(NewsAPI_org().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
